# Ellensburg GC GS Pro Course Build - Python Dependencies
#
# Core LIDAR/GIS processing
rasterio>=1.3.0
numpy>=1.24.0
scipy>=1.10.0
shapely>=2.0.0
pyproj>=3.5.0
fiona>=1.9.0

# PDAL (install via conda or system package manager, not pip)
# conda install -c conda-forge pdal python-pdal
# OR: brew install pdal (macOS)

# GDAL (install via system package manager)
# brew install gdal (macOS)
# GDAL Python bindings:
GDAL>=3.6.0

# GPX processing (uses stdlib xml.etree - no extra dependency)

# Optional: for enhanced progress bars
tqdm>=4.65.0

# Optional: for Jupyter notebook exploration
# jupyter>=1.0.0
# matplotlib>=3.7.0
# folium>=0.14.0
